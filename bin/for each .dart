void main() {
  //foreach  loop
  //collection.foreach(void f(value))
  var mental = ['mentel', 'dental', 'sundal', 'kundal'];
  mental.forEach((var num) => print(num));
  mental.forEach((element) => print(element));
}
